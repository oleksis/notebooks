language: python
python:
  - "3.7"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - py.test --nbval consumir_api_usuarios_live.ipynb
# check output: fixing
#  - py.test --nbval customsearch_api_pywombat_live.ipynb
